
test('clickImg', () => {
  it('', () => {
        let clickImg = [];
    expect(clickImg[0].getAttribute('id') === clickImg[1].getAttribute('id')).tobe(true)
  });
});

test('clickImg', () => {
    it('', () => {
          let clickImg = [];
      expect(clickImg[0].getAttribute('id') === clickImg[2].getAttribute('id')).tobe(false)
    });
});


test ('root', () => {

});

//expect(el instanceof HTMLElement).toBe(true);
